<% if @cart_items.present? %>
  $('#cart-<%= @cart_item.id %>').remove();
<% else %>
  $('#no_cart').html("<%= j(render 'customer/cart_items/no_cart') %>")
<% end %>
$('#flash').html("<%= j(render 'layouts/flash') %>");
$('#flash').show();
setTimeout("$('#flash').fadeOut('slow')", 2000);
